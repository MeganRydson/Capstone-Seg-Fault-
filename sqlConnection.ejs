var mysql = require('mysql');
var con = mysql.createConnection({
    host     : 'db-segfault-cap.cae0l6rwojdw.us-east-1.rds.amazonaws.com',
    port     : '3306',
    user     : 'Segfaultcapstone',
    password : 'S3gfault2019',
    database : 'db-segfault-cap'
});
con.connect(function(err) {
  if (err) throw err;
  console.log("Connected!");
  var sql = "INSERT INTO Event_Tenders (ten_Description, ten_Amt, ten_OpenAmt,ten_Taxable) VALUES ?";
  var values = [
    ['T-Shirts', '15.00','false','true'],
    ['Candy', '5.00','false','true'],
 
  ];
  con.query(sql, [values], function (err, result) {
    if (err) throw err;
    console.log("Number of records inserted: " + result.affectedRows);
  });
});